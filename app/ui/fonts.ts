import { Inter, Abel } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const abel = Abel( {subsets: ['latin'], // Specify subsets as needed
    weight: '400', // Specify weights if applicable
    display: 'swap',
 }); // Optional: Specify font display);
